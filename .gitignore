# Nothing of the dataset enters the repository. The site is built in CI and
# deployed straight as a Pages artifact: committing it would grow git history by
# hundreds of MB per build with no way to reclaim it.
dist/
.work/

__pycache__/
*.py[cod]
.DS_Store
